pipelineymlgen:
  output:
    - file: publish-config-prod.yml
      data:
        prod: true
    - file: publish-config-nonprod.yml
      data:
        prod: false
---
# This pipeline template injects the publish config for the
# production (prod) or non-production (nonprod) environment.

# This is based on eng/pipeline/stages/publish-config-prod.yml (and nonprod) but
# "$(public-mirror.server)" is removed from RegistryAuthentication because we
# don't manipulate it, only read it. We don't want to include its values, and
# fake values aren't accepted in this case, so we clone this file to omit it.

parameters:
# By default, images are staged in repos that are prefixed with this pipeline
# build ID. This is makes it easy to look up which pipeline images were built
# from and vice versa.
- name: sourceBuildPipelineRunId
  type: string
  default: $(Build.BuildId)

# This prefix is added to the staging repo when pushing images. If the trailing
# slash is omitted, it will not be added automatically.
- name: stagingRepoPrefix
  type: string
  default: "build-staging/"

# Images will have this prefix added to their repo name when pushed to the
# publishing ACR. If the trailing slash is omitted, it will not be added
# automatically.
- name: publishRepoPrefix
  type: string
  default: "public/"

# This template will have the publishConfig, internalProjectName, and
# publicProjectName parameters passed to it automatically.
- name: stagesTemplate
  type: string

# These parameters will be passed to the template referred to by the
# stagesTemplate parameter.
# Note: publishConfig, internalProjectName, and publicProjectName are passed
# automatically by this template. Don't define them in this parameter - they
# will get overwritten.
- name: stagesTemplateParameters
  type: object
  default: {}

# Enable container image signing
- name: enableSigning
  type: boolean
  default: false


stages:
- template: ${{ parameters.stagesTemplate }}
  parameters:
    ${{ insert }}: ${{ parameters.stagesTemplateParameters }}

    internalProjectName: "internal"
    publicProjectName: "public"

    # publishConfig schema is defined in src/ImageBuilder/Configuration/PublishConfiguration.cs.
    # This will get converted to JSON and placed in appsettings.json to be loaded by ImageBuilder at runtime.
    publishConfig:
      InternalMirrorRegistry:
        server: ${ ternary "$(acr-staging.server)" "$(acr-staging-test.server)" .prod }
        repoPrefix: $(internalMirrorRepoPrefix)

      PublicMirrorRegistry:
        server: $(public-mirror.server)
        repoPrefix: $(publicMirrorRepoPrefix)

      BuildRegistry:
        server: ${ ternary "$(acr-staging.server)" "$(acr-staging-test.server)" .prod }
        repoPrefix: "${{ parameters.stagingRepoPrefix }}${{ parameters.sourceBuildPipelineRunId }}/"

      PublishRegistry:
        server: ${ ternary "$(acr.server)" "$(acr-test.server)" .prod }
        repoPrefix: "${{ parameters.publishRepoPrefix }}"

      RegistryAuthentication:
        - server: ${ ternary "$(acr-staging.server)" "$(acr-staging-test.server)" .prod }
          resourceGroup: ${ ternary "$(acr-staging.resourceGroup)" "$(testResourceGroup)" .prod }
          subscription: ${ ternary "$(acr-staging.subscription)" "$(testSubscription)" .prod }
          serviceConnection:
            name: ${ ternary "$(build.serviceConnectionName)" "$(build-test.serviceConnectionName)" .prod }
            id: ${ ternary "$(build.serviceConnection.id)" "$(build-test.serviceConnection.id)" .prod }
            clientId: ${ ternary "$(build.serviceConnection.clientId)" "$(build-test.serviceConnection.clientId)" .prod }
            tenantId: ${ ternary "$(build.serviceConnection.tenantId)" "$(testTenant)" .prod }
        - server: ${ ternary "$(acr.server)" "$(acr-test.server)" .prod }
          resourceGroup: ${ ternary "$(acr.resourceGroup)" "$(testResourceGroup)" .prod }
          subscription: ${ ternary "$(acr.subscription)" "$(testSubscription)" .prod }
          serviceConnection:
            name: ${ ternary "$(publish.serviceConnectionName)" "$(publish-test.serviceConnectionName)" .prod }
            id: ${ ternary "$(publish.serviceConnection.id)" "$(publish-test.serviceConnection.id)" .prod }
            clientId: ${ ternary "$(publish.serviceConnection.clientId)" "$(publish-test.serviceConnection.clientId)" .prod }
            tenantId: ${ ternary "$(publish.serviceConnection.tenantId)" "$(testTenant)" .prod }

      cleanServiceConnection:
        name: ${ ternary "$(clean.serviceConnectionName)" "$(clean-test.serviceConnectionName)" .prod }
        id: ${ ternary "$(clean.serviceConnection.id)" "$(clean-test.serviceConnection.id)" .prod }
        clientId: ${ ternary "$(clean.serviceConnection.clientId)" "$(clean-test.serviceConnection.clientId)" .prod }
        tenantId: ${ ternary "$(clean.serviceConnection.tenantId)" "$(testTenant)" .prod }

      testServiceConnection:
        name: ${ ternary "$(test.serviceConnectionName)" "$(test-nonprod.serviceConnectionName)" .prod }
        id: ${ ternary "$(test.serviceConnection.id)" "$(test-nonprod.serviceConnection.id)" .prod }
        clientId: ${ ternary "$(test.serviceConnection.clientId)" "$(test-nonprod.serviceConnection.clientId)" .prod }
        tenantId: ${ ternary "$(test.serviceConnection.tenantId)" "$(testTenant)" .prod }

      Signing:
        Enabled: ${{ parameters.enableSigning }}
        ImageSigningKeyCode: ${ ternary "$(microBuildSigningKeyCode.containers)" "$(microBuildSigningKeyCode.testing)" .prod }
        ReferrerSigningKeyCode: ${ ternary "$(microBuildSigningKeyCode.attestations)" "$(microBuildSigningKeyCode.testing)" .prod }
        # Use signType 'real' even for non-prod to actually sign with the test certificate.
        # The 'test' signType skips signing entirely on linux; the test keycode provides a non-production certificate.
        SignType: real
        TrustStoreName: ${ ternary "supplychain" "test" .prod }
